// Placeholder translation unit — replaced in M0.0 Task 5 (final query assertion guard).
// Keeps starling_core linkable until final_query_assertion is implemented.
// A non-inline definition is required so the archive entry has at least one
// symbol; otherwise ranlib warns about empty translation units.
namespace starling::detail {
extern const int final_query_assertion_placeholder;
const int final_query_assertion_placeholder = 0;
}  // namespace starling::detail
